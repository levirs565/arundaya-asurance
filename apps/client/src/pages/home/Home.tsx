import { AppBar, AppBarTitle } from "@client/components/app-bar";

export function HomePage() {
    return <div>
        <AppBar>
            <AppBarTitle>Arundaya Assurance</AppBarTitle>
        </AppBar>
    </div>
}